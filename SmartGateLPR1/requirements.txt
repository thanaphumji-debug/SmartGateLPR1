# แพ็กเกจของฝั่ง AI (lpr_api.py) — ติดตั้งด้วย:  pip install -r requirements.txt
#
# ⛔ ลง paddlepaddle รุ่น CPU เท่านั้น อย่าลง paddlepaddle-gpu
#
# lpr_api.py รวม YOLO (torch) กับ PaddleOCR (paddle) ไว้ในโปรเซสเดียวกัน
# ถ้า paddle เป็นรุ่น GPU จะพังตอน import ด้วย
#     ImportError: generic_type: type "_gpuDeviceProperties" is already registered!
# เพราะ torch กับ paddle รุ่น GPU ต่างก็ผูก struct ของ CUDA เข้ากับ Python
# ผ่าน pybind11 โดยใช้ชื่อชนิดข้อมูลเดียวกัน และ pybind11 มีทะเบียนชนิดข้อมูล
# ชุดเดียวต่อหนึ่งโปรเซส ใครโหลดทีหลังจึงลงทะเบียนซ้ำไม่ได้
#
# YOLO (torch) ยังใช้ GPU ได้ตามปกติถ้ามีการ์ดจอ — ที่ต้องเป็น CPU คือ
# paddlepaddle (ตัวเดียว) เท่านั้น

# ---- ตรวจจับป้าย ----
# ต้อง >= 8.3.0 ขึ้นไป ถึงจะรองรับโมเดล YOLO11 (รุ่นก่อนหน้ารองรับแค่ถึง YOLOv8/v10)
ultralytics>=8.3.0
torch>=2.0
torchvision>=0.15

# ---- อ่านตัวอักษรบนป้าย ----
# PaddleOCR 3.x ใช้ API แบบ ocr.predict() และมีโมเดลภาษาไทย th_PP-OCRv5_mobile_rec
paddleocr>=3.0.0
paddlepaddle>=3.0.0

# ---- อื่น ๆ ----
flask>=3.0
opencv-python>=4.8
numpy>=1.24
